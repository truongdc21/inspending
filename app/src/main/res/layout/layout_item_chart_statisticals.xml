<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingStart="10dp"
    android:paddingEnd="10dp"
    android:orientation="vertical"
    android:gravity="center_horizontal|bottom"
    android:layout_height="match_parent">

    <View
        android:id="@+id/itemViewChart"
        android:layout_width="15dp"
        android:layout_height="20dp"
        android:background="@color/colorf3" />

    <androidx.cardview.widget.CardView
        android:id="@+id/itemChartCardView"
        android:layout_marginTop="10dp"
        android:layout_width="20dp"
        android:layout_height="20dp">
            <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/viewItemIconViewChart"
                android:background="@color/colorf5"
                android:layout_width="match_parent"
                android:layout_height="match_parent">
                    <ImageView
                        android:id="@+id/imgItemChartCategory"
                        app:layout_constraintTop_toTopOf="parent"
                        app:layout_constraintLeft_toLeftOf="parent"
                        app:layout_constraintRight_toRightOf="parent"
                        app:layout_constraintBottom_toBottomOf="parent"
                        android:layout_width="15dp"
                        android:layout_height="15dp"
                        android:backgroundTint="@color/white"
                        android:background="@drawable/ic_monney"
                        />
            </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</LinearLayout>